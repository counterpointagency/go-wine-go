/* ═══════════════════════════════════════════════════════════════════
   GO WINE GO — PAGE — notfound.js
   The error document needs nothing beyond the shared core: header,
   search, drawer, age gate and toast. It has its own entry point so
   every page loads exactly one module and none loads a page's code
   it has no use for.
   ═══════════════════════════════════════════════════════════════════ */
import '../core.js';
